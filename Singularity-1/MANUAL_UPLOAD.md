# Singularity-1 landing page: manual upload

Upload every file in this ZIP to the matching path in the repository `stpaul2coderdojo/Singularity-oneproject-`, preserving the folders.

Changed files:

- `index.html`
- `src/index.css`
- `src/main.tsx`
- `src/LandingPage.tsx`
- `vite.config.ts`
- `.github/workflows/deploy-pages.yml`

After committing the files, open **Repository Settings → Pages** and set **Source** to **GitHub Actions**. The deployment workflow will publish the page at:

`https://stpaul2coderdojo.github.io/Singularity-oneproject-/`

The repository path is dictated by its exact name; GitHub Pages URLs cannot contain spaces.
